# Living Docs — documentation drift

**Repository:** frenbrentest/testrepo
**Commit:** [`4301fa9`](https://github.com/frenbrentest/testrepo/commit/4301fa9088e367c1ed618d646351e0dab579f5f6)
**Detected at:** 2026-05-18T12:27:02.177Z

## Summary

| File | Symbol | Commit | Reason |
| --- | --- | --- | --- |
| `utils.ts` | formatDate, calculateTotal | [4301fa9](https://github.com/frenbrentest/testrepo/commit/4301fa9088e367c1ed618d646351e0dab579f5f6) | Documentation drift detected for formatDate (description now inaccurate, missing locale param) and calculateTotal (incorrect description, param name, return type). Updated JSDoc to match current code. |

## `utils.ts`

| Field | Value |
| --- | --- |
| Symbol | formatDate, calculateTotal |
| Project ID | `IH5dEidPRp9YzEtZ05zk` |
| Drift doc ID | `utils.ts` |
| File at commit | [view](https://github.com/frenbrentest/testrepo/blob/4301fa9088e367c1ed618d646351e0dab579f5f6/utils.ts) |

### What drifted

Documentation drift detected for formatDate (description now inaccurate, missing locale param) and calculateTotal (incorrect description, param name, return type). Updated JSDoc to match current code.

### Before

_No stored living doc — drift detected from inline JSDoc in the diff._

### After (proposed JSDoc)

~~~js
/**
 * Formats a Date object into a locale-specific date string.
 * @param date - The date to format.
 * @param locale - Optional locale string (default "en-US").
 * @returns A string representing the date in the specified locale.
 */
/**
 * Formats an array of prices as strings with two decimal places.
 * @param prices - An array of numbers representing prices.
 * @param discountRate - (Deprecated) This parameter is no longer used.
 * @returns An array of formatted price strings.
 */
/**
 * Clamps a number between a minimum and maximum value.
 * @param value - The number to clamp.
 * @param min - The minimum allowed value.
 * @param max - The maximum allowed value.
 * @returns The clamped number.
 */
~~~
