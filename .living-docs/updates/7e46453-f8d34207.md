# Living Docs — AI analysis

This pull request was opened automatically from commit `7e46453`.

Review the proposed documentation below and merge or copy changes into your source files as needed.

## `utils.ts`

- **Symbol:** clamp
- **Changed:** true
- **Drift detected:** false

### Reason

The exported function clamp had its logic changed to also require the number to be finite. Updated JSDoc to reflect the new behavior.

### Proposed documentation

~~~ts
/**
 * Checks if a number is within a specified range, inclusive, and is finite.
 * @param n - The number to check.
 * @param lower - The lower bound of the range.
 * @param upper - The upper bound of the range.
 * @returns `true` if `n` is between `lower` and `upper` (inclusive) and is finite, otherwise `false`.
 */
~~~
