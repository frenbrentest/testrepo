# Living Docs — AI analysis

This pull request was opened automatically from commit `5b1203b`.

Review the proposed documentation below and merge or copy changes into your source files as needed.

## `utils.ts`

- **Symbol:** formatDate
- **Changed:** true
- **Drift detected:** true

### Reason

The formatDate function's return type changed from string to number, and its implementation now returns date.getTime() instead of a formatted string. The inline JSDoc still claims it returns a string in YYYY-MM-DD format, which is now inaccurate. The existing documentation for calculateTotal remains correct.

### Proposed documentation

~~~ts
/**
 * Returns the timestamp of the given date.
 * @param date - The date to get the timestamp for.
 * @param locale - The locale string (unused in current implementation).
 * @returns The timestamp as a number (milliseconds since epoch).
 */
~~~
