# Living Docs — AI analysis

This pull request was opened automatically from commit `9bb9c64`.

Review the proposed documentation below and merge or copy changes into your source files as needed.

## `utils.ts`

- **Symbol:** clamp
- **Changed:** true
- **Drift detected:** true

### Reason

The function's return type changed from number to string, and the logic now returns a descriptive string instead of a clamped number. The inline JSDoc still describes the old behavior (returns 'The clamped number') and is now inaccurate.

### Proposed documentation

~~~ts
/**
 * Clamps a number between a lower and upper bound, returning a descriptive string.
 * @param n - The number to clamp.
 * @param lower - The minimum allowed value.
 * @param upper - The maximum allowed value.
 * @returns A string indicating the clamped value, or "out-of-range" if n is not finite.
 */
~~~
